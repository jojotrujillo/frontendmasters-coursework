// Note the square brackets around the slug parameter. This is a special syntax to create dynamic routes in Next.js.
// The slug parameter is a special name for the file system path of the page inside the pages directory.
